# Terraform Backend Configuration

This directory will contain remote backend configuration using:

- S3 for state storage
- DynamoDB for state locking

Backend will be configured before provisioning infrastructure.
